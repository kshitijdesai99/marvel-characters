metastore -> catalog -> schema -> table/view/volume/function

dev, acc, prod
3 different catalogue with their own schema
dev writes to dev catalogue

no manual push -> CI/CD pipeline
worspace securables, unity catalog securables
ACLs (Access Control lists)

Every action is audit-logged